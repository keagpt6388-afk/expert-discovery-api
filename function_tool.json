{
  "type": "function",
  "name": "find_experts_for_research",
  "description": "Finds field experts and returns identity-resolution seed data for a patent and publication search orchestrator.",
  "parameters": {
    "type": "object",
    "properties": {
      "field": {"type": "string", "description": "Research or technology field."},
      "context": {"type": ["string", "null"], "description": "Optional narrowing context."},
      "max_candidates": {"type": "integer", "minimum": 1, "maximum": 10}
    },
    "required": ["field", "context", "max_candidates"],
    "additionalProperties": false
  },
  "strict": true
}
